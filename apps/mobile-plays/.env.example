# Override the API the app reads from (defaults to expo.extra.apiUrl in app.json).
EXPO_PUBLIC_API_URL="http://localhost:4400/v1"
# Which tenant's works the app shows.
EXPO_PUBLIC_TENANT_SLUG="baburao"
